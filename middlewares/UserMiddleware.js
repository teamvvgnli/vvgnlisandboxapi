exports.isUserAdmin = (req, res, next) => {
  // logic to check if user is admin user or not
};

exports.isUserNormalUser = () => {
  // logic to check if user is normal user or not
};

exports.doesUserHasRole = (role) => {
  // logic to check if user has specific mentioned role or not
};
